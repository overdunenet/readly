# Readly 프로젝트 작업 환경

## 기본 환경

- **Node.js**: v22 (`.nvmrc` 참조)
- **Package Manager**: Yarn (v4.x)
- **Language**: TypeScript (strict mode)

## 개발 서버 포트

| 서비스     | 포트 |
| ---------- | ---- |
| API        | 3000 |
| Web App    | 5173 |
| Backoffice | 5175 |

## 환경 변수

```bash
# API
DATABASE_URL=postgresql://...
JWT_SECRET=...

# Frontend
VITE_API_URL=http://localhost:3000
```

## 스킬 (Skills)

작업별 상세 가이드는 `.claude/skills/` 폴더를 참조하세요:

| 스킬          | 경로                               | 설명                      |
| ------------- | ---------------------------------- | ------------------------- |
| API 개발      | `.claude/skills/api/SKILL.md`      | tRPC + NestJS 백엔드 개발 |
| Frontend 개발 | `.claude/skills/frontend/SKILL.md` | React 프론트엔드 개발     |
| 공통 패턴     | `.claude/skills/common/SKILL.md`   | 공통 코딩 패턴            |

> **주의**: 스킬 수정 시 `.claude/hooks/skill-forced.sh` 파일도 함께 수정하세요.

## Context

배경지식은 `.claude/context/` 폴더를 참조하세요:

| Context  | 경로                            | 설명                     |
| -------- | ------------------------------- | ------------------------ |
| 아키텍처 | `.claude/context/architecture/` | 시스템 구조, DB 스키마   |
| 도메인   | `.claude/context/domain/`       | 비즈니스 로직, 기능 명세 |

## 빠른 참조

### 빌드/린트

```bash
yarn lint          # ESLint 실행
yarn typecheck     # TypeScript 체크
yarn build         # 프로덕션 빌드
```

### DB 작업

```bash
yarn db:migrate    # 마이그레이션 실행
yarn db:seed       # 시드 데이터 삽입
```

<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

_No recent activity_
</claude-mem-context>
